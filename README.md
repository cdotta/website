Screenshots referenced from pull request descriptions.

This branch holds images only — it is never merged into `main` and
contains none of the site's source. Captured with Playwright against
`astro dev`, dark theme, 2x device pixel ratio.
